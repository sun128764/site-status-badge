{
	"name": "site-status-badge",
	"version": "1.0.0",
	"description": "A Cloudflare Worker that generates SVG badges showing website status",
	"private": true,
	"scripts": {
		"generate-webp": "tsx src/generate-webp.ts && tsx src/upload-webp.ts",
		"deploy": "pnpm generate-webp && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@types/node": "^25.0.3",
		"sharp": "^0.34.5",
		"tsx": "^4.21.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.56.0"
	}
}